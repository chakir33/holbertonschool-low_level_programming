#include "main.h"
#include <stdio.h>

/**
 * puts_half - code
 *
 * @str : pointeur
 */
char *leet(char *)
{
	int lon = 0;
	int moit;

	while (str[lon] != '\0')
		lon++;
	for (moit = lon / 2; moit < lon; moit++)
	{
	_putchar(str[moit]);
	}
	_putchar('\n');
}
