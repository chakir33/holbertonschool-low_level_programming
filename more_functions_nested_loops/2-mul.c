#include "main.h"
#include <stdio.h>

/**
 * mul - mult of number
 * @a : numero 1
 * @b : numero 2
 * Return: Always the reresult
 * sum : its the result
 */
int mul(int a, int b)
{
	int sum = a * b;
return (sum);

}
