#include "main.h"
/**
 * main - return hours
 *@c : 123
 * Return: Always 0.
 */
void jack_bauer(void)
{
	int h = 0;
	int m = 0;
for (m = 0; m < 24; h++)
{
for (h = 0; h < 60; m++)
}
_putchar('0' + h / 10);
_putchar('0' + h % 10);
_putchar(':');
_putchar('0' + m / 10);
_putchar('0' + m % 10);
_putchar('\n');
}
